description: 取消拉黑一名用户
parameters:
  - name: target_username
    in: body
    type: string
    required: true
    example: 'xuhb20'
responses:
  200:
    description: 成功
  401:
    description: 当前处于未登录状态
  500:
    description: 失败，并返回错误原因
